
const button=document.querySelector('.menu-toggle');const menu=document.querySelector('#menu');button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!open));menu.classList.toggle('open',!open)});menu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{menu.classList.remove('open');button.setAttribute('aria-expanded','false')}));const now=new Date();document.querySelector('#year').textContent=now.getFullYear();document.querySelectorAll('[data-days]').forEach(row=>{if(row.dataset.days.split(',').map(Number).includes(now.getDay())){row.classList.add('is-today');row.setAttribute('aria-current','date')}});

